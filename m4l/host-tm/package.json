{
  "name": "@stencil/host-tm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "M4L host layer for Stencil TM. Bridges Max transport and the Turing engine: owns step state, resolves step→note, shapes MIDI events. Loaded by [node.script] in Stencil-TM.amxd via index.js; unit-tested standalone under node --test.",
  "main": "./dist/host-tm/host.js",
  "scripts": {
    "test": "node --test",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  }
}
