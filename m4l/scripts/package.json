{
  "name": "@pointsman/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bake pipeline for Pointsman M4L (.maxpat -> .amxd) and patcher guard tests.",
  "scripts": {
    "test": "node --test",
    "build": "echo 'no build for scripts package'",
    "typecheck": "echo 'no typecheck for scripts package (plain JS)'"
  }
}
