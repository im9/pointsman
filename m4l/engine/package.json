{
  "name": "@pointsman/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Quantizer engine for Pointsman. Runs in Node.js for tests and (via dist/) in Max for Live [node.script] for the device.",
  "scripts": {
    "test": "node --test",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  }
}
